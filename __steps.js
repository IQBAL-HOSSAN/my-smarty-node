/* 
    1. create a folder (manually or mkdir)
    2. open command line to that folder
    3. npm init
    4. npm install express
    5. create index.js
    ------------------
    For express/node server
    ------------------
    1. require express
    2. create app variable
    3. declare port
    4. set app.get("/")
    5. listen to port
    6. node index.js
    7. check your browser for that port
    8. install nodemon to restart node application

    ------------------------

    ------------------------
    1. install cors to solve fetch error in own api. and declare a variable and require the cors like const cors = require('cors');
    2. Then call the cors into the app.use(cors())
    3. To get req.body in post method => install body-parser and call like abode cors or use app.use(express.json())
    4. 



*/
